2025-06-14 先学一下 unity(54小时的课, 要2天) 141/270 (有点慢啊, 要加油了)
 + 可以通用 mklink /j path1 2path 软链目录, 不要忘了把 path1 的只读属性去了
 + Edit > Project Settings > Tags and Layers 新加 Tag
 + C# 单文件可以写多个并列类
 + 其他类上加 `[Serializable]` 可以在主类里作为成员变量时, 在 editor 里实例化一个对象
 + 不要忘了在 Package Manager 安装 Visual Studio Editor 功能
 + Project Setting -> Input Manager 中, 可以管理一些输入设置, 功能有有限
 + Rule Tile 之类也要在 Package Manager -> 2D Tilemap Extras 安装
 + 2d 以 y 轴为基础的渲染方式里, 可以在 Project Setting -> Graphics 里设置, 也在可以使用配置文件的方式, 让 unity 直接去读配置文件, Transparency Sort (Costom Axis x0 y1 z0)
 + Project Setting -> Time 可以设置一些 FixedUpdate 的参数
 + Layer 可以影响碰撞, 不同层的话, 如果要不影响, 要在 Project Setting -> Physics 2D 的 Layer Collision Matrix 里改


HybridCLR + YooAsset + QFramework

QFramework 学习中 14/56

https://www.gamepixedu.com/course/50/task/5393/show 这个要再看一下, 没有消化